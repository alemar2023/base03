<!DOCTYPE html>
<html>
<head>
  <title>Geko Base03</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
</head>

<body>
<%= render "/layouts/header" %>
<main class="container">
  <% if notice %>
    <p  class="notice" style="margin: 0;padding:0;color:green;"><%= notice%>
  <% elsif alert %>
    <%= alert %></p>
  <% end %>

  <h1><%= @cookie_value %></h1>
  <h1>xx<%= @cookie_due %></h1>
  <h2 style="color:red">Session: <%= @session_value %></h2>
  <%= yield %>
</main>
</body>
</html>
